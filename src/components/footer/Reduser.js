// export const initialState = {
//   son: 0,
//   strr: "whats ur name",
//   data: [],
// };
// export const reduser = (state, action) => {
//   switch (action.type) {
//     case "INC":
//       return { ...state, son: state.son + action.payload };
//     case "DEC":
//       return { ...state, son: state.son - 1 };
//     case "NATION":
//       return { ...state, strr: (state.strr = action.laylo) };
//     default:
//       return state;
//   }
// };
